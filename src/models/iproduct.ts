export interface IProduct { 
    id: String, 
    name: String, 
    imageUrl: String, 
    price: String,
    quantity: Number,
    categoryId: Number,
    availability: boolean,
    vendorId: Number
}
